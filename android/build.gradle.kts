/* Версии собраны в одном месте нарочно: их несовместимость — самая частая
   причина «не собирается», и разбираться в ней проще, когда все числа рядом.

   AGP 8.13 (а не 9.x) выбран сознательно: девятая ветка требует Gradle 9.5 и
   приносит свои перемены в поведении, а нам нужна не свежесть, а сборка с
   первого раза. Поднять успеем. */

plugins {
    id("com.android.application") version "8.13.0" apply false
    id("org.jetbrains.kotlin.android") version "2.1.21" apply false
}
